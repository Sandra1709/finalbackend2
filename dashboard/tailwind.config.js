/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{html,ts}", // Ensure Tailwind scans all HTML and TypeScript files for classes
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
